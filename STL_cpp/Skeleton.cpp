#include <bits/stdc++.h>   // Includes all standard C++ libraries
using namespace std;       // Lets us use standard names without "std::"

int main() {
    int a;                 // Declare an integer variable
    cin >> a;              // Take input from user
    cout << a << endl;     // Print the value of 'a'
    return 0;              // End program successfully
}
